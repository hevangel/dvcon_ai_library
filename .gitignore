.env
.env.*
!.env.example

.cursor/
.zcode/

/data/

backend/.venv/
backend/.pytest_cache/
backend/.mypy_cache/
backend/__pycache__/
backend/.ruff_cache/

frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/.env.local

# docs/ workspace: GitHub Pages serves docs/index.html, so the HTML is
# committed. Keep the curated CSVs + generator script in git too. Ignore the
# venv, pycache, and the large derived per-paper tables (re-generatable).
docs/.venv/
docs/__pycache__/
docs/data/per_year_*.csv
docs/data/reproducibility.csv

# llm-wiki: keep the markdown pages + extractor + manifest in git.
# The _sources/ dir is large (50 JSON files with abstracts + chunks) and
# regeneratable from the DB, so ignore it.
llm-wiki/_sources/

*.pyc
*.pyo
*.pyd
*.log
*.tmp
*.swp
.DS_Store
